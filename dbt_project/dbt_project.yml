name: de_reference
version: '1.0.0'
config-version: 2
profile: de_reference
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target","dbt_packages"]
models:
  de_reference:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: ephemeral
    mart:
      +materialized: table
      +schema: mart